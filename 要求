以上是已设计好的前端页面代码。请保留这些页面的结构和视觉风格，只补充交互逻辑和数据对接。技术栈：前端部署Vercel，后端Node.js/Express部署Render，数据库Supabase，AI模型DeepSeek API。后端所有接口以/api开头，前端API_BASE设为https://daisy-home-api.onrender.com。数据库需创建sessions、messages、reactions、events、diary表，Supabase Storage创建avatars桶。环境变量：SUPABASE_URL、SUPABASE_KEY、DEEPSEEK_API_KEY。请生成完整的index.html、server.js、package.json、manifest.json、sw.js文件。
实现以下所有功能：PWA配置，manifest.json的display设为standalone，注册service worker，可安装为桌面应用全屏使用。会话列表页支持新建、删除、置顶会话。聊天页发送消息后调用DeepSeek API获取AI回复，消息实时存入数据库。长按消息弹出菜单包含编辑、复制、删除、引用回复、Emoji反应。AI消息支持重新生成（替换当前回复）和切换版本（保留历史版本可来回切换）。模型切换下拉菜单可切换DeepSeek和DeepSeek R1，切换后对话使用对应模型。设置页包含消息通知开关、独处提醒开关、聊天背景更换（预设颜色或上传图片）、字体大小调节（标准/大/特大三档）、夜间模式切换、导出聊天记录为JSON文件、清空所有消息、关于页面（显示版本号和daisy & xiaod）。头像可点击上传更换，用户和AI头像均可上传至Supabase Storage的avatars桶，上传后立即更新显示。主页包含在一起天数（从2026年6月11日起算，动态计算并显示）、事件提醒（可设置提醒内容，到达时间通过ntfy推送）、日历（支持月份切换，标注经期、排卵期、纪念日，点击日期查看当日历史消息）、共享日记（全屏弹窗编辑，双向可写，用户和AI均可写入，按日期翻阅）、快捷入口按钮。聊天页面输入框旁加一个图片按钮，点击可上传图片，后端调用视觉模型识别图片内容并回复图片描述。AI回复支持图片生成，我描述画面后通过DALL·E或Midjourney API生成图片返回前端显示。所有功能需完整实现，代码可直接部署运行。